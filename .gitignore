node_modules/
dist/
.env
*.db
*.db-journal
.DS_Store
# Local pg_dump backups (e.g. taken before a migration) — real production
# data, never belongs in git history.
*.dump

# graphify knowledge-graph output (tool artifact, regenerable)
graphify-out/
